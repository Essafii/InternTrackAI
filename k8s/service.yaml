apiVersion: v1
kind: Service
metadata:
  name: interntrackai-backend-service
spec:
  selector:
    app: interntrackai-backend
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: NodePort